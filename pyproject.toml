[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nutriexpert"
version = "1.0.0"
description = "Sistema Experto de Nutrición con IA"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = []
